# Project Documents

Documents are grouped by use:

- `planning/`: experiment plans, improvement plans, and implementation checklists
- `reports/`: final report material and experiment result summaries
- `presentations/`: final deck files and speaking scripts
- `progress_reports/`: historical checkpoint PDFs used as project context

Report notebooks live under `../notebooks/reports/`, and reusable report figures live under `../figures/`.
